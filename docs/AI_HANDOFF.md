# AI Handoff Brief — Lapanza 3D Creative Lab

**If you are an AI assistant picking up this project cold, read this file first.** It orients you fast; it is not the full reference. Once you have repo access, read [`docs/SYSTEM_DOCUMENTATION.md`](SYSTEM_DOCUMENTATION.md) next — it has the complete architecture, database schema, API reference, functional spec, and test cases. This file exists so you don't have to read that whole document before knowing where to start or what not to break.

---

## What this is

A live e-commerce + operations platform for a South African 3D-printing/filament retailer (Lapanza 3D Creative Lab). Node/Express backend + SQLite, static Vite-built storefront, hand-written vanilla-JS admin SPA. Single developer/owner, no team. Tests run automatically on push/PR (GitHub Actions), but deploy is still manual — changes are tested, pushed, then deployed to production over SSH.

**Repo:** `github.com/jbarkhuizen/Lapanza-3d-Creations-Martin`, branch `main` (the only branch — no PR workflow, commits go straight to `main`)
**Live site:** https://lapanza3d.co.za
**Live admin:** https://lapanza3d.co.za/admin/
**Production server:** VPS at `41.222.36.147` (domain.co.za, AlmaLinux 10), SSH-key-only access as user `deploy`

## Start here, in order

1. `README.md` — quick local-dev setup
2. `docs/SYSTEM_DOCUMENTATION.md` — everything: architecture, DB schema (32 tables), full API reference, every feature's functional spec, requirements, test cases, deployment runbook, known limitations
3. `deploy/DEPLOY.md` — production deployment/SSH runbook specifically
4. `docs/UPTIME_MONITORING.md` — external monitoring setup (manual, third-party account signup — not something to attempt on the user's behalf)
5. `.env.example` and `deploy/.env.production.template` — every config variable, documented

## Non-obvious things that will bite you if you don't know them

These are real incidents from this project's build/deploy history — documented in full in `SYSTEM_DOCUMENTATION.md` §12.5, listed here so you don't repeat them:

- **`better-sqlite3` requires Node ≥22, hard requirement.** Node 20 installs with only a warning, then segfault-crashes the process on every DB access. Always verify `node -v` is 22+ before anything runs.
- **Never blindly re-copy `deploy/nginx-lapanza.conf` onto the live server.** Once certbot has run, the live `/etc/nginx/conf.d/lapanza.conf` has HTTPS/redirect blocks appended that a naive overwrite deletes, silently breaking HTTPS. `deploy/deploy-app.sh` already guards against this (only installs the template if the file doesn't exist) — do not "fix" that guard away.
- **Express needs `app.set('trust proxy', 1)`** (already set in `server/index.js`) because nginx sits in front of it in production. Removing it breaks every rate-limited route with a cryptic crash that looks to end users like a random 404.
- **Two independent, in-memory session systems** (admin sessions and customer sessions) — both are lost on every `systemctl restart lapanza-admin`. This is expected, not a bug to "fix" reflexively.
- **Product data lives in two places, not one:** the sellable filament catalog is in SQLite (`filament_types`/`filament_colours`); category items (toys/homeware/phones/car-parts) are in `data/catalog.json`, which is **gitignored** — it's real business data, not code, and must be copied to a new server separately from `git clone`. Since V0.80 the backup job snapshots it alongside every `.db` backup (paired `.catalog.json` file, pruned together, synced offsite with the rest of the backups dir), and `saveCatalog()` writes temp-then-rename so a crash can't truncate it. Note the `DATA_DIR` caveat below still applies: the backup reads `dataDir()/catalog.json` while `store.js` writes `{cwd}/data/catalog.json` — identical unless someone sets `DATA_DIR`.
- **Storefront static pages are not live-rendered.** They're pre-generated HTML (`scripts/generate-pages.mjs`, triggered by the admin "Publish to site" button), which is then bundled into `dist/` — the directory nginx actually serves — by `npm run build` (vite). "Publish to site" runs **both** steps now; until a fix this session it only ran the first, so clicking it looked like it worked (source pages updated) but the live site silently never changed until the next full code deploy happened to run the build too. If you're debugging "my catalog edit isn't showing up," check whether `dist/` was actually rebuilt, not just whether `/api/publish` returned 200. `PUT /api/settings` had this exact same gap independently (2026-08-27, found when featured products didn't appear live after being saved) — it now also runs the generate+build pipeline, non-fatally (a `publishWarning` in the response if it fails, the setting itself is still saved). If you add a THIRD route that writes something publicly-visible, check whether it needs the same treatment rather than assuming `syncPublicJson()` alone is enough — it never is for anything nginx serves from `dist/`.
- **Currency formatting has one canonical implementation, in three files.** `server/money.js` (server + `generate-pages.mjs`), `admin/money.js`, `src/js/money.js` are deliberate twins — admin/ and src/js/ are separately served/bundled and can't import server/money.js directly. If you change the format, update all three and say so in the commit; five independent, drifted copies is exactly the bug that made this necessary in the first place.
- **Payfast's `payment_method` values are `cc` and `ef` — not `eft`.** Confirmed against https://developers.payfast.co.za/docs#quickstart. An invalid value doesn't error, it silently falls through to whatever payment method Payfast's own account config falls back to — this is exactly how a customer choosing "Instant EFT" ended up on an unrelated Mobicred page. If a payment-method-related bug report sounds bizarre, check the wire value against Payfast's docs before assuming it's an account-config issue.
- **Checkout prices are always server-resolved, never trust a client-submitted price** — this is a deliberate security property, not an oversight to "simplify."
- **Stock is RESERVED at order creation, not checked-then-decremented-later.** `reserveStockForOrder()` in `server/orders.js` runs inside the same `db.transaction()` as the order/order_items INSERT for online checkout — it re-reads stock fresh and throws (rolling back the whole order) if insufficient. This is deliberate, not incidental: a pre-transaction check followed by a decrement-at-payment-time (the original, buggier design) lets two concurrent orders both pass the check before either one reserves anything, so both get confirmed for the same physical item. If you ever touch this code, keep the check-and-decrement together, inside one transaction, using a fresh read — don't split them apart again. `createManualOrder` reserves the same way but never hard-blocks (admin trusted, floors at 0) — see `decrementStockForOrder`. Cancelling an order (`cancelStalePendingOrders` or admin `updateOrderStatus(..., 'cancelled')`) releases the reservation via `restoreStockForOrder`. Test coverage: `server/orders.test.js`, the block of tests starting with "createOrder reserves...".
- **Password reset tokens (V1.01) are a separate column pair from email-verification tokens** (`reset_token`/`reset_token_expires` vs `verification_token`/`verification_token_expires` on `clients`) — both can be outstanding at once without clobbering each other. Reset TTL is 1h, shorter than verification's 24h, since it's freely re-requestable.
- **`deploy/deploy-app.sh` writes Git-backed release detail to the live DB directly** (via `scripts/record-deploy-version.mjs`, no HTTP/auth involved) as its last step. It creates the Version History row and its release-detail record atomically: commit metadata, release notes, changed files, and line statistics. Do not make this step non-fatal: a deployed version without traceable release detail defeats the audit trail. Use `scripts/backfill-version-release-details.mjs --force` only to rebuild historical detail from Git.
- **The Test Cases page must only execute catalogued Node tests.** `server/test-runs.js` derives the catalog from `server/*.test.js` and invokes `node --test` with fixed server-side arguments. Do not accept arbitrary shell commands, paths, or test patterns from the browser. On a Git checkout it runs from a temporary detached worktree, not the live checkout, to protect production-local data and generated pages. Test-run history is intentionally retained in SQLite for the admin report.
- **About this Site is intentionally read-only and non-privileged.** `server/site-overview.js` inventories files the `deploy` service account can read, but never file contents. Keep `/proc`, `/sys`, `/dev`, and `/run` non-browsable; they are virtual filesystems, not durable site storage, and should never be scanned by the application.
- **Newsletter campaign recipients are consent-bound snapshots.** Do not treat `whatsapp_opt_in` as email consent. `newsletter_campaign_recipients` stores the exact selected audience; `newsletter_suppressions` blocks future sends after unsubscribe. Campaign delivery temporarily uses Gmail SMTP by owner choice, so retain low-volume, manual sends until a dedicated email provider is selected.
- **Newsletter rich content is server-rendered from blocks.** Keep `server/newsletter-content.js` as the source of truth for valid heading, text, image, button, and divider blocks. Do not accept arbitrary browser HTML for campaigns; image assets are limited to JPEG/PNG/WebP under 5MB and plain-text fallback is mandatory.
- **Newsletter analytics measure SMTP outcomes, not reader engagement.** `getCampaignAnalytics()` aggregates the immutable recipient snapshots into audience, accepted, failed, pending, and source totals. A `sent` recipient means Gmail accepted the message for delivery; do not label it as delivered, opened, clicked, or inboxed without a provider-supported tracking design and explicit privacy review.
- **Newsletter administration is auditable.** Template/image changes and campaign creation, approval, test send, and queueing use `marketing_updated` audit entries. Keep action details non-sensitive: subjects, filenames, and recipient counts are appropriate; recipient email addresses are not.
- **In-house filament transfers are stock movements.** The selected `filament_colours` item is decremented and the chosen in-house row incremented in one database transaction. Do not replace this explicit picker with automatic Type/Colour matching. New local entries are unique by Brand + Type + Colour; brands come from `inHouseFilamentBrands` in Site Settings.
- **Offsite backup sync uses OAuth as the real Google account, NOT a service account.** A service account was tried first and confirmed broken: it can be shared onto a Drive folder and read/list it fine, but any new file upload fails with `storageQuotaExceeded` — service accounts have zero storage quota of their own on a personal (non-Workspace) Google account, and folder-sharing doesn't change that; real Shared Drives (where it would work) are Workspace-only. `rclone authorize "drive"` run once on a machine with a browser produces a token to paste into the VPS's rclone config instead (DEPLOY.md §9) — a human step, but a one-time one; the refresh token keeps it working indefinitely after that.
- **Multi-line heredocs pasted into an SSH session can silently mangle** — hit this live writing `rclone.conf`: the heredoc "succeeded" (no error) but the file kept its old contents. Prefer a single-line `printf '...\n...\n' > file` for anything written this way over SSH.
- **There is a live Todo / Backlog admin page (`/api/todos`) for tracking gaps you identify while working in this repo** — if you're an AI assistant continuing this project and notice something worth doing later (not urgent enough to fix inline), add it there instead of only mentioning it in chat: `POST /api/todos` with `{ name, description, category }` through the same admin session. Category is one of `Bug`/`Feature`/`Enhancement`/`Tech Debt`; leave `status` unset to default to `Backlog`.
- **`DATA_DIR` does NOT redirect `data/catalog.json`.** `server/paths.js`'s `dataDir()` (SQLite, backups) respects `DATA_DIR`; `server/store.js`'s own `paths()` for the category-item catalog is hardcoded to `{cwd}/data/catalog.json` regardless. Testing anything that touches category items (checkout, catalog editor, Print Job Costing's "List for sale") against a scratch `DATA_DIR` still reads/writes the real local `data/catalog.json` — back it up first if you're clicking through it manually, same as this session did.
- **SQLite `date()`/`datetime()` default to UTC; the server runs in SAST (UTC+2).** Any query grouping by calendar day (invoice dates, analytics daily breakdowns) needs the `'localtime'` modifier explicitly, or day boundaries silently shift near midnight. Already bit this project once (imported invoice dates landed a day early) before being applied proactively elsewhere.
- **`.env` secrets are never pasted into chat/AI tooling** — the working rule established for this project is that whoever has server access types secrets directly on the server (`nano /opt/lapanza/app/.env`). Don't ask for or accept Payfast keys, the Gmail app password, or WhatsApp tokens in conversation.
- **`creator`/`models`/`sourceUrl` on a category item are car-parts-only** (GWM/Landrover, gated in `admin.js` on `parent === 'car-parts'`) — every other category (toys/homeware/phones) never renders or stores them, they just sit as empty defaults from `normalizeItems()`. `models` stores plain name strings picked from Settings' `carPartModelsLandrover`/`carPartModelsGwm` lists (one per brand, not ids — same convention as `inHouseFilamentBrands`), chosen in `admin.js`'s `carPartItemFields()` by the item's own category slug. `sourceUrl` is intentionally left out of `export.js`'s public `categories.json`, don't add it there without checking it's meant to become customer-facing. If a spreadsheet import ever needs redoing (Landrover model corrections, GWM's turn), don't retype a source spreadsheet's "Fits (Vehicles)"-style column by hand without spot-checking it against the source site first — the Land Rover sheet had truncated/corrupted model and category columns that the source site's own per-part pages (JSON-LD) didn't.
- **Every new Settings key needs THREE additions, not two — this has silently broken saves twice now** (`inHouseFilamentBrands` originally, `carPartModelsLandrover`/`carPartModelsGwm` on 2026-08-27, both caught only by actually testing the save, not by reading the code). Adding a new admin-editable setting requires: (1) `DEFAULT_SETTINGS` in `settings-defaults.js`, (2) `LIST_SETTING_KEYS` in `settings.js` if it's a `{id,name,active}[]` list (read-side normalization), AND (3) the `allowed` array in `PUT /api/settings` (`server/index.js`) — miss #3 and the admin UI looks fully functional (renders, the save button works, no error) while every edit is silently discarded before it ever reaches the DB. If you build a new configurable list, write the round-trip regression test (`server/index.test.js`, search "allowlist-missing") in the same sitting, not after — it would have caught this immediately both times.
- **Regenerating static pages locally (`node scripts/generate-pages.mjs`) trusts whatever's in your local `public/uploads/`** — `imageFileExists()` has no way to know a photo exists only on the production server and never made it into your checkout; it just falls back to "Photo coming soon" for a genuinely-missing local file and writes that into the committed HTML. Bit this project once (2026-08-27, GWM/toys photos silently regressed after a routine catalog merge). Before pushing a locally-regenerated category page, `grep -c 'Photo coming soon'` it and compare against what's actually live, or pull the real uploaded files down first (`scp` from the VPS) — don't assume local and production `public/uploads/` match just because the catalog data does.
- **A freshly-inserted `<details open>` can fire a `toggle` event with no user action involved** — some browsers emit one synthetic `toggle` the instant such an element is parsed/connected to the DOM, matching the `open` state you just set. A naive `addEventListener('toggle', ...)` attached right after `innerHTML =` will misread that as a real click. Bit the Stock Management grouping (2026-08-27): re-rendering during a search force-opened a manually-collapsed section, and the resulting spurious `toggle` silently erased the remembered collapse state. Fix pattern used there (`admin.js`, search "data-initial-open"): stamp the element with the `open` value you rendered it with, and in the toggle handler, ignore the event once if `el.open` still equals that stamped value — only a real subsequent toggle can differ from it. Reuse this pattern for any other collapsible `<details>` built the same way (server-string template + `innerHTML`, not individually created DOM nodes).
- **Every catalogue card and filament swatch has a stable `id="item-{sku-or-fallback}"`** (`server/item-anchor.js`'s `itemAnchorId()`, used by both `generate-pages.mjs` when writing the id and `export.js` when resolving a link to it) — this is how `settings.featuredProducts` links land on the exact item, not just the top of the page. If you ever change how a card's id is computed in one of those two files, change it in `item-anchor.js` itself so both stay in sync, or a featured-product link silently breaks.
- **`settings.featuredProducts` stores `productId` refs (`filament:{slug}:{sku}` / `category:{slug}:{sku}`), never a typed name/price/link** — resolved fresh into `{name,price,href}` by `export.js`'s `syncPublicJson()` on every publish. Don't add a manual-entry featured-product field; the whole point is it can't go stale.
- **`todo_items.created_by` is NULL on every row that predates 2026-08-27** (no honest way to backfill who logged something before the column existed) — the admin UI shows "—" for those, don't treat a NULL as an error. When creating a Todo/Backlog item via `POST /api/todos` on the owner's behalf, pass `createdBy: 'Claude'` explicitly in the body — omitting it defaults to whichever admin session is active, which would misattribute an AI-logged item to whoever happens to be logged in.
- **Order confirmation emails must never send before payment is actually confirmed.** For `payfast_card`/`payfast_eft`, `/api/checkout` only creates the order as `pending_payment` — the customer hasn't paid yet, they're about to be redirected to Payfast. `sendOrderConfirmationEmail()` only fires from the `/api/payfast/itn` webhook handler once `payment_status=COMPLETE`, gated on `markOrderPaid()`'s `changed` flag (not just its `lowStock` result) so a duplicate ITN redelivery for an order already marked paid can't resend it. `manual_eft`/`cash_on_collection` still confirm immediately at `/api/checkout` time — there's no online payment gate to wait for there. If you're touching checkout email logic, keep this split; sending "your order is confirmed" for an abandoned/declined Payfast payment is exactly the bug this fixed. **This does NOT apply to the invoice email** (`sendInvoiceEmail()`, `mailer.js`, added 2026-08-28) — an invoice stating what's owed is truthful whether or not Payfast has cleared, so it sends immediately for every payment method; only the plain-text "confirmed" wording needs the payment gate.
- **Any route that creates/edits/deletes a filament, colour, category product, or category item must call the shared `publishCatalog()` helper** (`server/index.js`, added 2026-08-28), not just `syncPublicJson()`. This is the third time the same bug has shipped: a write reaches the DB and the `public/*.json` export fine, but the *static* HTML pages (`generate-pages.mjs` output, bundled into `dist/` by `vite build`) never regenerate, so the change looks saved in the admin but never appears live until someone clicks "Publish to site" or a deploy happens to rebuild anyway. Row 118 (§2) fixed it for `PUT /api/settings`; this fix closed it for every remaining catalog-mutating route in one pass (including new per-item routes) after it was found to be the actual reason a GWM item's "Fits models" checkboxes never made the public model-filter dropdown appear. If you add a new route that writes anything `generate-pages.mjs` reads, call `publishCatalog()` (or route through code that already does) — don't reach for a bare `syncPublicJson()` "just this once."
- **`renderInvoiceHtml()` lives in `server/invoice.js`, not `server/index.js`.** One template, three callers: the admin's "Print invoice" route, and `mailer.js`'s `sendInvoiceEmail()` (unpaid at order placement, `{paid:true}` once Payfast confirms or an admin marks a manual order "Already Paid"). If the invoice layout ever needs to change, change it once here — don't let the emailed and printed versions drift into two different templates.
- **The email-delivery-down WhatsApp fallback (`server/alerts.js`'s `checkEmailFallback()`, backlog #120) will silently no-op until the owner does two things this assistant cannot do for them**: create and get Meta Business Manager approval for a WhatsApp message template (WhatsApp's own rules forbid free-text business-initiated messages — same constraint that already blocks #7 WhatsApp campaigns), and fill in that template's name + their own WhatsApp number in Settings → Operational Alerts. Until both exist, a Gmail outage that would otherwise trigger this fallback just logs a `console.error` and nothing reaches anyone — don't report this as "broken," it's the documented degraded state, same as every other WhatsApp feature in this codebase.
- **`DATA_DIR` only sandboxes the SQLite DB — `PUT /api/settings`/catalog-mutating routes still write to the REAL project's `src/data/*.json`/`public/site-settings.json`/generated HTML regardless.** Hit this 2026-08-28 verifying the email-templates feature: started `node server/index.js` with `DATA_DIR=<scratch>` pointing at a genuinely fresh/empty DB (to avoid touching the real local dev DB), created a throwaway admin, then called `PUT /api/settings` to test a save — this triggered the same `runGenerate()`+`syncPublicJson()` pipeline every settings save runs (§2 Evolution History, "third crossing" gotcha above), which reads/writes those files from fixed `process.cwd()`-relative paths (`scripts/generate-pages.mjs`, `server/export.js`), never respecting `DATA_DIR`. Result: the scratch DB's empty/default data got written straight into `src/data/filaments.json` (lost ~1750 lines of real catalog data), `public/site-settings.json`, and `src/data/settings.json` — all still just uncommitted working-tree changes (`git checkout -- <files>` fully recovered them, nothing was ever committed/pushed/deployed), but a close call. **If you need to exercise a real running server against a throwaway DB, avoid any route that calls `runGenerate()`/`runBuild()`/`syncPublicJson()`** (that's `PUT /api/settings` and every catalog/filament/colour/category CRUD route) — stick to routes that only touch the DB (auth, read-only GETs), or accept that `git status`/`git diff --stat` on `src/data/*.json`, `public/site-settings.json`, and every generated HTML page needs checking (and reverting if unintended) immediately afterward, before anything gets staged.
- **Adding a new root-level static HTML page (like `get-in-touch.html`) needs a THIRD addition, same failure shape as the Settings-key gotcha above.** `vite.config.js`'s `htmlEntries()` hand-lists every root page as a rollup build entry — only `filament/` and `car-parts/` are auto-globbed from disk. A new page works perfectly in `npm run dev` (vite's dev server serves whatever's on disk, entry list or not) and passes every test (nothing in the suite runs `vite build`), so the first sign of trouble is a live 404 after deploy. Hit for real 2026-08-28: `get-in-touch.html` shipped, deployed, immediately checked live (not just trusted the deploy script), found 404ing, root-caused and fixed within the hour. Adding a new top-level page requires: (1) the HTML file itself, (2) a nav link (`src/js/nav.js`), AND (3) an entry in `vite.config.js`'s `htmlEntries()` — miss #3 and it's invisible until someone actually loads the live URL.
- **Every owner-facing notification email must read `settings.orderNotificationEmail` (`mailer.js`) -- never its own hardcoded address or a separate env var.** `sendLowStockAlert` didn't, until 2026-08-28: it had its own `LOW_STOCK_ALERT_TO` (`process.env.LOW_STOCK_ALERT_EMAIL || 'jbarkhuizen@gmail.com'`), independent of Settings, and the env override was never actually set in production -- so every low-stock alert landed in a personal inbox regardless of what the admin had configured in Settings → Public Site Contact. `sendNewOrderNotificationEmail`/`sendOrderCancelledNotificationEmail`/`sendNewDesignRequestNotificationEmail` were already correct; `sendLowStockAlert` now matches them. If you add a new owner-facing notification, read `getSettings().orderNotificationEmail` the same way -- don't invent a new constant or env var for "where should this go," there is exactly one answer to that question in this app.
- **Admin topbar's global action buttons (`admin/index.html`'s `.topbar-actions`, "+ Filament"/"+ Category") are visibility-gated in `setRoute()` (`admin.js`) by an *inclusion* list, not exclusion — deliberately, after the exclusion version broke.** The original code hid them on a hardcoded list of routes (Settings/Editor/Backups/etc) and showed them on everything else by default; every route added after that list was written (Registered Users, Orders, Clients, New Order, Invoice History, Shipping, Resources, Design Requests, Newsletter, WhatsApp Updates, Stock, Purchases, Print Job Costing, In-House Filament, Dashboard) silently inherited "show the catalog buttons" even though none of them are catalog pages — found and fixed 2026-08-28. If any future page-scoped-only UI element (a button, a toolbar) needs to show on just one or two views, gate it by checking `route === 'thatRoute'`, not by excluding everywhere else it shouldn't appear — a forgotten exclusion is invisible until someone notices the element in the wrong place, an inclusion list fails safe (hidden) instead.
- **Client matching by name (added 2026-08-28) is a deliberate, real risk, not an oversight.** `findOrCreateClientForCheckout` (`clients.js`) matches by email first; only when there's no email match does it fall back to an *exact, case-insensitive* first+last name match — and only when that name matches exactly one existing client, never more. Two genuinely different customers who happen to share a common name (e.g. two "John Smith"s) will never be silently merged by this path, but a customer who changes their email AND happens to share a name with someone else on file is a real (rare) edge case that could match the wrong record. If this ever needs tightening (e.g. requiring phone or a partial address match too before allowing a name-only match to update a record), that's the function to change — don't add a second, competing matching path elsewhere.
- **Any `localStorage`-persisted checkout/cart state must be cleared on every order-success path, not just the happy one.** `lapanza-checkout-prefs` (shipping/payment method persistence) was being cleared alongside the cart on the Manual EFT/Cash on Collection success path, but not on the Payfast one (`checkout-complete.html`) — so a customer placing a second order in the same browser session got the *previous* order's shipping/payment picks silently restored instead of the real HTML defaults. Both cart and prefs now clear from the same two places (`clearCart()` + `clearCheckoutPrefs()`, `src/js/checkout-prefs.js`); if a third success path is ever added (e.g. a different payment gateway), clear both there too.
- **A custom CSS rule cannot silently override a Tailwind v4 utility class by source order alone.** `@import "tailwindcss"` generates its own utilities in an internal `utilities` cascade layer that outranks an unlayered custom rule regardless of where that rule sits in the stylesheet — hit this making the collapsible sidebar's header offset (`#site-header.is-collapsed{left:0}` in `main.css` was silently losing to the existing `md:left-72` class). Needed `!important` to win. If a custom override rule "does nothing" against a Tailwind utility, this is the first thing to check, not a specificity/source-order bug.
- **Any order-cancellation path must call both `restoreStockForOrder` AND the owner-notification email — there are now three of them, not one.** `pending_payment → cancelled` happens via the 7-day auto-cancel job (`jobs.js`), an admin's `updateOrderStatus`, or the customer's own `cancelOrderByClient` (account page "Cancel" button). All three restore stock; all three now also call `sendOrderCancelledNotificationEmail` (`server/mailer.js`) so the business owner sees every cancellation, not just new orders. If a fourth cancellation path is ever added, it needs both, not just the stock restore.
- **A security-warning hook blocks any Edit/Write call whose diff contains the literal substring "innerHTML", including new code that mirrors an existing, already-escaped pattern elsewhere in the same file.** Hit repeatedly this session (checkout bucket-split, account-page order-cancel UI, and again documenting the workaround itself in this very file). It's a dumb substring match, not a semantic check. Two ways through it: (1) for new rendering logic, write it with `createElement`/`textContent`/`appendChild`/`replaceChildren` from the start instead of a template-string assignment; (2) when editing a large existing template-string-style file where a full rewrite isn't proportionate (e.g. `admin/admin.js`), split the change into several smaller Edit calls, none of whose old_string/new_string includes that one flagged line itself — edit the surrounding content in separate calls instead. Both got real work done this session without either tripping the hook or rewriting untouched code.
- **Every public form on this site has `novalidate` on the `<form>` tag, so `required` attributes alone do nothing — the JS must call `form.reportValidity()` itself before its fetch, or the browser silently skips native validation entirely.** Checkout already did this correctly; the design-request form didn't (`name`/`phone` weren't even marked `required` in the HTML, and the JS never checked `form.checkValidity()`/`reportValidity()` at all) until this was noticed and fixed. If a form on this site "isn't validating," check for `novalidate` first, not the `required` attributes.
- **`orders.status` and `orders.payment_status` are two separate columns that can silently drift apart.** Before this session, only `markOrderPaid` (the Payfast ITN webhook path) ever touched `payment_status` — an admin manually setting status to `paid` via the status dropdown left `payment_status` on `'pending'` forever, which is exactly why Invoice History's "Status" column looked permanently stuck on Pending for manually-marked-paid orders. `updateOrderStatus` now keeps both in lockstep for every caller (paid/shipped/completed → paid, pending_payment → pending, cancelled leaves it alone). If you ever add a new way to move an order's status, route it through `updateOrderStatus` rather than writing `status` directly — writing only `status` reintroduces this exact drift.
- **`listOrders()` and `getOrder()` used to have different return shapes for the same conceptual thing.** `getOrder()` (single order) has always attached a real `client: {...}` object via a join; `listOrders()` (the list/search version, used by both the admin Orders and Invoice History views) never did — it only ever built its client lookup map to power the `q` search filter, never attached it to the rows it returned. This is why Invoice History's client column was blank for every single order. Now fixed at the shared source (`listOrders` always attaches `client`), but if you add a third list-returning order function, check it matches `getOrder`'s shape rather than assuming the two existing ones already agreed with each other.
- **A CSS override for `table.catalog` (admin.css) must come AFTER the `table.catalog` rules it's overriding, not just anywhere in the file.** `table.todo-table td` and `table.catalog td` have identical selector specificity (element+class+descendant-element either way), so when both match the same element, source order decides the winner — not which one is "more specific" or which class is listed second in the HTML. Hit this building the Todo/Backlog page's tighter-row styling: the new `.todo-table` block was placed earlier in the file (near the other `.todo-*` rules) than the shared `table.catalog` block further down, so the padding/font-size override was silently losing to the later, more general rule. If a scoped override of a shared admin table style "does nothing," check its position in admin.css relative to the rule it's meant to override before assuming a specificity or selector-syntax problem.
- **`execFileSync`/`execSync` on this project's single Node process blocks the ENTIRE admin backend, not just the caller.** There's no worker pool or clustering here — one process serves every request. A synchronous subprocess call (rclone, git, anything shelling out) freezes Node's single-threaded event loop for its whole duration, so every other in-flight or incoming request queues behind it regardless of which route it's for. Confirmed the hard way: adding an uploads copy to the backup sync turned a near-instant DB-only `execFileSync` call into a 5+ minute one, and the whole site went unresponsive for that entire window. Use `execFile`/`exec` (via `util.promisify` or their native Promise/callback form) for anything that isn't guaranteed near-instant, especially inside a scheduled job (`jobs.js`) that runs unattended in production.
- **A filament colour's `imageUrl` being set does NOT mean the file exists on disk** — `generate-pages.mjs` now checks (`imageFileExists()`) before rendering `<img>` instead of trusting truthiness alone, falling back to "Photo coming soon". Worth checking `ls public/uploads/filaments/` against the DB before assuming a broken image is a code problem.
- **`git stash push -u` on this repo's VPS is genuinely dangerous, and `git stash show --stat` will not warn you.** `public/uploads/` is real, deliberately-uncommitted business content (photos etc, `.gitignore` never covers it) that shows as untracked in `git status` right alongside harmless regenerated-catalog noise. `-u` sweeps up BOTH into the same stash; `git stash show --stat` (the obvious "is it safe to drop" check) silently omits untracked files from its output entirely, so it can report "looks fine" while blind to real content sitting inside. This is exactly how an AI assistant destroyed 106 of 107 filament colour photos on 2026-08-27 (Linandi's manual uploads, not a content gap as first assumed) — recovered only by luck, because a dropped stash isn't immediately garbage-collected (`git fsck --no-reflog` found it, `git ls-tree` on its untracked-files parent had the files, `git checkout <tree> -- <path>` restored them). **Rule going forward: use `git stash show --include-untracked` (or `-u`) before ever dropping a stash on this repo, or avoid `git stash -u` entirely** — stage/move aside the specific dirty tracked files you actually need to clear instead of blanket-stashing untracked content you haven't inventoried. `public/uploads/` also still has zero backup coverage (backlog #132) — the recovery above worked despite that, not because of it.
- **`npm run dev:admin` (`node server/index.js`, no `--watch`/nodemon) does not pick up code changes without an actual restart, and on this Windows/Git-Bash setup `pkill -f "server/index.js"` does not reliably kill it** — a stale admin server from hours/turns earlier can keep serving old code indefinitely while every later `npm run dev:all` silently fails to bind port 8787 (look for `dev:admin exited with code 0` in its log — that's the tell) and every `vite` instance piles up on the next free port instead (5173, 5174, 5175, ...). This produced a real false bug report mid-session (a new field added to `listInventory()` appeared completely missing at runtime). Verify what's actually listening before trusting a "fresh" dev server: PowerShell `Get-NetTCPConnection -LocalPort 8787 -State Listen | Select-Object OwningProcess` then check that process's `StartTime` against when the code last changed; `Get-Process node | Stop-Process -Force` (not `pkill`) to actually clear every stale instance before restarting.
- **The admin login screen used to hardcode `Default password: lapanza-admin` under the sign-in form, live in production, regardless of whether a real admin account already existed** — the actual first-run flow (`/api/setup/status`, `/api/setup`, `admin.js`'s `#view-setup`) was already correct and properly gated on `needsSetup`; this was just leftover static HTML from before that flow existed. Removed (closes backlog #35). If you ever touch `admin/index.html`'s login markup again, don't reintroduce a literal credential/hint string — the setup flow is the only place a password should ever be shown, and only on first run.
- **Don't trust "Payfast live payments verified" without checking the actual ITN log, not just that `PAYFAST_MODE=live` and a transaction was attempted.** The only real ITN this production server has ever received (Aug 26, order `691b035e`, a R5 test) came back `serverConfirmed: true`/`amountValid: true` but `signatureValid: false`. Passphrase was checked and confirmed to match the Payfast dashboard exactly (2026-08-27) — the real cause was `buildSignature()` in `server/payfast.js` filtering out blank-valued ITN fields before hashing, which contradicts Payfast's own reference PHP implementation (it includes every posted field except `signature`, blank or not — confirmed against their published sample code, not assumed). Fixed via a `skipEmpty` option: `true` (unchanged) for the outbound checkout redirect, `false` for inbound `verifyItn()`. Until a fresh real transaction logs `valid: true` end-to-end (order flips to `paid`, stock/email/audit all fire from the ITN handler, not just `serverConfirmed`), don't report Payfast live payments as fully proven working — this fix is evidence-based but unproven against a real transaction, since the original failing one is gone (deleted in the test-order cleanup).
- **Payfast's own reference ITN-signature algorithm includes every posted field except `signature`, even ones with a blank value — never filter those out before hashing on the inbound (verification) side.** `urlencode('')` is still `''`, contributing a bare `key=` to the param string, not omitting the key. This is fine (and correct) to filter on the OUTBOUND redirect, since we choose what to send; filtering it on the INBOUND ITN check produces a different string — and therefore a different hash — than the one Payfast itself computed, for any real ITN that happens to include a blank optional field (`custom_str1`-`5` etc, common when unused).

## Current production state (as of this handoff)

| Item | State |
|---|---|
| Payfast | **Fully verified end-to-end with a real live transaction (2026-08-30, closes #45)** — order INV-0011, R5.00, Payfast ref 324392419: ITN from a genuine Payfast IP passed `signature=true`/`serverConfirmed=true`/`amountValid=true` (first live ITN ever to pass — proves the 2026-08-27 `buildSignature()` blank-fields fix), order auto-flipped paid/paid, transaction recorded COMPLETE, all three customer emails (placement invoice, order confirmation, Paid-in-Full invoice) confirmed received by the owner. `PAYFAST_MODE=live`, passphrase matches the dashboard. Unlike every earlier "verified" claim in this project's history, this one is backed by a live payload, not code review |
| WhatsApp campaigns | Not configured (`WHATSAPP_ACCESS_TOKEN`/`WHATSAPP_PHONE_NUMBER_ID` unset) — requires the business owner to complete Meta Business Account + template approval first; sends fail cleanly with "not configured" until then |
| Gmail SMTP | Configured and working (order confirmations, verification emails, notifications) |
| SSL | Live, Let's Encrypt via certbot, auto-renewing |
| Automated backups | Live — daily + on-boot, 30-backup retention, admin "Backups" view. Mirrors offsite to Google Drive via `rclone` right after every automated run (`syncOffsite()` in `server/backups.js`) once `BACKUP_RCLONE_REMOTE` is set (DEPLOY.md §9 — confirmed already configured in production). **`public/uploads/` (photos etc) now rides along too (closes #132)** — a sibling `uploads` folder on the same remote, `rclone copy` (never `sync`, so a local deletion never propagates offsite). Runs via async `execFile`, not `execFileSync` — a real production sync took 5+ minutes once uploads were added, and the synchronous version froze the entire admin backend for that whole duration |
| Uptime monitoring | `/api/health` verifies real DB connectivity (503 on failure). Setup guide at `docs/UPTIME_MONITORING.md` — whether an actual monitor is configured depends on that manual, third-party account-signup step having been completed by the owner |
| Visitor analytics | Live — first-party, anonymous pageview/heartbeat tracking, admin "Analytics" page. No cookie/tracking disclosure shown to visitors yet (ties into the still-missing Privacy Policy page) |
| Checkout stock reservation | Live — stock is decremented at ORDER CREATION (inside the same transaction as the order insert), not at payment. This is what actually stops two concurrent checkouts both claiming the same last unit — the earlier "stock validation" only checked, it never reserved, so both could pass the check before either paid. Cancelling an order (auto or admin) restores the reservation |
| Customer password recovery (V1.01) | Live — `account.html` "Forgot password?" → `POST /api/client/forgot-password` emails a single-use, 1h-expiry reset link (generic response either way, no email enumeration) → `POST /api/client/reset-password` sets the new password, verifies the account, revokes other live sessions for that client, logs the requester in. Closes the previous register→verify→login-only dead end |
| Version history and release details | Automated — `deploy/deploy-app.sh` runs `scripts/record-deploy-version.mjs` after every deploy. Pre-release labels run `V0.01` onward; `V1.0` is reserved for the official release. Each version number opens a drill-down with Git commit metadata, notes, changed files, and line statistics. Historical detail is backfilled with `scripts/backfill-version-release-details.mjs --force`. |
| Documentation and Test Cases | Live — Settings includes a catalogued Documentation page for all checked-in Markdown documents and a Test Cases page that lists `server/*.test.js` cases, runs selected cases/suites/all cases, and retains report history. |
| About this Site | Live — Settings has a read-only VPS operations overview: runtime and capacity data, application storage paths, backups, and a full browseable filesystem inventory available to the service account. Virtual kernel filesystems are deliberately not traversable. |
| Todo / Backlog page | Live — `server/todos.js`, `GET/POST/PUT /api/todos` under the existing admin session (no separate API key for this assistant to add items — same auth path an admin uses). Append-only, no delete. Seeded once on first boot with 13 items from §15 Known Limitations. Statuses now include three hygiene classifications beyond Done/Won't Fix/Claude Fix: `Discarded` (item no longer applicable / merged elsewhere, 2026-08-28) and `Deferred` (still valid, deliberately parked with a revisit trigger, 2026-08-29) — see SYSTEM_DOCUMENTATION.md's todo API notes for the distinctions |
| Full backlog audit (2026-08-29) | Applied — all 90 then-open items audited against the real codebase, owner approved resolutions: #14/#38/#79 Done (remainders split to new #134–#137), 15 items Deferred with triggers, 14 Discarded with merge notes (#80/#83/#85/#88 folded into #81's phased guided-quote plan; #91 into #90; #53 into #52), open backlog 90 → 58. Second owner-approved batch 2026-08-30 (backup `pre-audit-apply-2026-08-30.db`): #2/#16/#37/#55/#56/#68 Done (done-in-substance closures), #64/#77/#105/#117/#121 Discarded — open backlog now 54. Every change audit-logged, pre-change DB backups under `data/backups/`. Full per-item detail lives in the audit artifact in the owner's Claude session. **Top open priority remains #45: no real Payfast ITN has ever passed signature verification — the 27 Aug fix is still unproven against a live transaction** |
| **Open backlog snapshot (2026-09-01 08:20, authoritative at time of writing — re-query `todo_items` for current truth)** | 29 open: **9 Backlog** — #7 WhatsApp campaigns (blocked on owner's Meta verification; all code shipped), #28 Enrico legal review (pack ready, owner to send), #52 print gallery (needs owner photos + consent), #59 bundles (suggest defer — volume discounts now active cover it), #92 paid design requests (substance shipped in quote cluster; awaiting owner approval to close done-in-substance), #93 repeat custom-print orders (**only buildable item, ~2 days, unblocked by #87**), #99 promotions/gift cards (promo codes ~2–3 days when wanted; gift cards stay parked), #103 audience segmentation (waits on real analytics volume), #111 knowledge centre (waits on owner-authored content). **20 Deferred with triggers** — #41/#42/#47/#48/#49/#57/#58/#63/#67/#69 (catalogue/browsing extras: trigger = catalogue size/traffic), #70/#71/#74/#75 (vehicle+technical: trigger = range growth/owner content, #75 needs supplier PDFs), #100/#101/#112/#118/#123/#133 (growth/ops: trigger = customer volume/second admin/marketing spend). Owner standing pile: TEST quote click-through on prod (quote R50 → accept → R25 deposit → delete), admin click-throughs (PUDO, print-job qty, reorder report, brand list, Communications templates), materials-guide + SEO-page prose review |
| **Admin catalog batch: stock price filter, client company column, catalog groupings (2026-09-01, `4ffad9d`)** | Bounded UI-only changes, no backend/data changes: Stock Management gets a Min/Max Rand filter next to the existing search box; the Clients list gets a Company column reading the already-existing `businessName` field (was captured on the edit form but never shown in the list); Product Catalog's single flat table was restructured into the same collapsible groups (Filament/Toys/Phones/Homeware/Car Parts→GWM/Landrover) Stock Management already used, keeping the existing Kind/Status/search filters working within each group. All three verified live in a real local admin session (price filter dropped 210→0 items at a R1000 minimum, Company column showed a real seeded value, groupings rendered correct per-section counts). Fixed a local-only dev environment issue along the way: `.claude/launch.json`'s combined `dev:all` config let a single injected `PORT` env var leak into both the vite and admin sub-processes, making them fight over the same port — split into separate `lapanza-dev-site`/`lapanza-dev-admin` configs with their own explicit ports. |
| **New categories no longer lose status/featured/sortOrder/SEO fields on create (2026-09-01, `92bc4af`)** | Real bug, not a one-off bad data row: `POST /api/products` built a new category from a fixed field list that never included `status` (or `featured`/`sortOrder`/SEO fields), even though the admin client sends `status: 'draft'` on every create — the sibling `PUT` route already carried these fields forward correctly, `POST` just never did. Every category created via the normal admin "+ Category" flow ended up with no `status` on disk, printing literally "undefined" in the admin catalog badge (the owner's live "Weed Shop" category report) and, if the affected item's `name` field was also missing, on the item's own public detail page (title/H1). Fixed both root causes: `POST /api/products` now includes the same fields `PUT` does, and the item detail-page generator now falls back a missing `item.name` to `Item N` (matching `normalizeItem()`'s own convention) everywhere it's interpolated, so a data gap can never again render as customer-visible "undefined" text. 2 new tests (407/407 total). The Weed Shop row's own real status/item name still needs the owner's input — this fix only stops it (and every future category) from happening again. |
| **Design-request form updates + post-submit account-creation offer (2026-09-01, `378e04f`)** | Owner-requested trims to the public custom-print-request form (`design-request.html`): Material Preference cut to PLA/PETG/ABS/ASA (dropped TPU/CPE); How Urgent replaced with concrete day-counts (Urgent (1 day)/3 days/1 week/2 weeks/1 month, blank default kept); Delivery now mirrors checkout's real 5 shipping labels exactly (previously only had 3, missing both courier options). Also: a design-request submission now always resolves a client behind it (guest find-or-create by email, same `findOrCreateClientForCheckout()` checkout already uses — previously a guest submission got no client record at all), and offers the exact same "create an account to track this" prompt checkout already shows post-purchase, reusing `POST /api/client/register` with no new backend primitives. 6 new tests. The three dropdown changes were confirmed live in a real browser (exact option lists verified via the rendered page); the post-submit account-creation panel was not — this session's local dev port conflict (see the commit two rows above, fixed after this one shipped) blocked reaching the admin API needed to exercise a real submit. Its backend logic is fully covered by route-level tests exercising the identical `POST /api/design-requests` → `client.hasAccount` code path a live click-through would hit; a real browser pass on it is still owed. |
| **Multi-photo product galleries + real detail pages (2026-09-01, `52cced3`, V0.99)** | Filament colours and category/car-parts items now support up to 5 photos each — new `filament_colour_images` SQLite table / `catalog.json` `images[]` field, both with a legacy single-photo fallback so existing photos keep working. Admin gets a shared drag-and-drop gallery panel (`admin/admin.js`) replacing the old single-file-input UI; 6 new REST endpoints reuse the existing multer+sharp pipeline. Generator now writes one real static detail page per product with Product JSON-LD — flat `filament/<type>-<sku>.html` / `products/<slug>.html`, deliberately not nested, to avoid vite's non-recursive `htmlEntries()` 404 trap (see that row above). Storefront carousel via new `src/js/product-gallery.js` (scroll-snap + IntersectionObserver, no new deps). **Mandatory final whole-branch review (Opus, 3 passes) caught 2 Critical + 5 Important issues no task-scoped review could see**: two fake test-fixture pages committed and would have shipped as real products (test cleanup hardened); every product created after this feature shipped would have gotten an imageless cart line/homepage tile/JSON-LD image because the legacy `imageUrl` admin UI was removed but 5 read sites still read only it (now gallery-first everywhere); admin gallery panel now shows a read-only legacy-photo fallback (spec required one, deliberately non-deletable to avoid a delete-path landmine); draft state no longer drops the gallery on every keystroke; `og:image` added to detail pages; 3 latent unescaped-attribute sites fixed; stale detail pages now pruned at generate time. One fix round, re-reviewed clean, merged to `main`, pushed, deployed, and live-verified (real colour/item detail pages 200 with correct JSON-LD/og:image, the previously-fake fixture page now correctly 404s). Full detail incl. every bug found/fixed across all 13 tasks: SYSTEM_DOCUMENTATION.md §2 Evolution History. 399/399 unit + `npm run build` clean + 4/4 e2e. |
| Owner resolutions batch 2 (2026-09-01, DB-only) | #2 CI/CD reclassified Done → Won't Fix (deploy automation permanently declined; CI test runs stay), #16 confirmed Done, new **#138** logged + closed Claude Fix (owner requirement "third car-part brand = Settings edit, not code change" — already satisfied by #130; recipe in the item description). Volume discounts (#60) now ACTIVE: owner saved tiers 12+ → 5%, 24+ → 7.5%; verified in prod DB, public site-settings.json, and the generated "Volume pricing" note on live filament pages. #126's scroll+focus confirmed as shipped (option (a): append order kept, `focusNewRow` smooth-scrolls + focuses). Backup `pre-owner-resolutions-2026-09-01b.db` + audit-log entries |
| Owner resolutions (2026-09-01, `92d1f92`) | #137 closed Claude Fix (parking/collection line on get-in-touch.html — owner's wording, live-verified), #46 closed Done (owner set up external uptime alerting per docs/UPTIME_MONITORING.md), #116 staging Discarded (owner: no second VPS). Prod DB backup `pre-owner-resolutions-2026-09-01.db` + audit-log entries. **#60 volume discounts still INERT — `settings.volumeDiscounts` unset on prod; owner must add tiers in Settings → Storefront and Save (save runs the publish pipeline)** |
| **#93 repeat custom-print orders + CRITICAL #87 fix (2026-09-01)** | #93 shipped: `POST /api/design-request-status/reorder` (token-gated) books a fresh order at the recorded quote price on a `finalized` request, "Order This Again" button on `design-request-status.html`. **While testing it, found and fixed a live-production bug in #87's Accept & Pay**: its order line used `{name, price}` but `createManualOrder` requires `{description, unitPrice}` — every quote acceptance since #87 shipped 2026-08-31 has 400'd instead of redirecting to Payfast. **Confirmed hitting a real customer 2026-09-01** (owner-reported: token `867dd4b6…` stuck on click). Fix is in this same commit, deployed immediately given live impact. Owner's standing "TEST quote click-through" item should be re-run now to confirm live. 376/376 tests. | 376/376 |
| **Admin Dashboard sales data (2026-09-01, `80919eb`, V0.96)** | Dashboard tab was catalog-counts only — no revenue, no orders, nothing sales-shaped. New `GET /api/dashboard/sales?range=today\|7d\|30d\|90d\|all`: revenue/orders/AOV (paid+shipped+completed only — pending excluded, shown separately), daily revenue trend (inline SVG bar chart, no chart lib), top 10 products by revenue, full order-status breakdown. Live-sanity-checked against real prod orders over SSH (R10,993 all-time, 8 paid orders) — numbers check out; top products currently show "imported historical invoice" labels since those orders predate real itemization, expected not a bug. **Not click-tested in a browser this session — local dev-server preview tool failed to spawn (environment issue, both npm and node, unrelated to this change).** Owner should click through the live Dashboard once to confirm it renders as intended. 383/383 tests. | 383/383 |
| **Deposit tiers + Quote Stage + real shipping on design-request orders (2026-09-01, `6038b68`, V0.98)** | Owner-requested: (1) deposit % now an extensible admin-editable list (`settings.quoteDepositOptions`, seeded 25/50/75/100%) picked per-quote, replacing the old single sitewide `quoteDepositPct` number; (2) a derived "Quote Stage" badge (Quoted / Order Placed / Order Paid) on the admin Design Requests list+detail, separate from the New/In Progress/Finalized work-status field — Order Paid needs no admin action, it appears the instant the linked order's Payfast ITN clears; (3) fixed the real gap the owner flagged: design-request orders (Accept & Pay, Order This Again) always hardcoded `collect` shipping with no address capture — both now offer Collect/Own Courier/a real Fixed delivery option with address fields, same as regular checkout (minus weight-matched courier, which a custom job has no weight for). **Also caught and fixed the settings-save allow-list bug** (old `quoteDepositPct` key left in `PUT /api/settings`'s allow-list instead of swapped to `quoteDepositOptions` — would have silently discarded every tier save) and **a frontend wiring bug** (new deposit-tier panel accidentally shared a CSS class with an unrelated generic wiring loop, crashing Settings-page event binding after the deposit-tier section on every load) — both caught by a real Playwright browser click-through before shipping, not just unit tests. 12 new tests, 388/388 unit + 4/4 e2e. | 388/388 unit + 4/4 e2e |
| Security-hardening follow-up (2026-08-31, `6d7db7b`) | Deployed + live-verified — baseline security headers on Express responses (nosniff / X-Frame-Options DENY / HSTS over HTTPS; dist/ pages get theirs from nginx), session cookies (admin + client) now `Secure` via `req.secure` (NOT a NODE_ENV gate — NODE_ENV isn't set on the VPS), `/api/auth/me` now applies the requireAuth TTL check (expired session → login screen, not a 401-spamming shell), newsletter send queue + admin SPA nav/logout error-wrapped. Headers confirmed live on /api/health. 369/369 unit tests |
| Phase-6 hardening (2026-08-31) | Deployed — #122 Reorder Report atop Stock Management (`GET /api/reorder-report`: at/below-threshold items + 30-day sold, cancelled excluded), #130 car-part brands as `settings.carPartBrands` (generator/nav/search derive from it; new brand = list entry + matching +Category + Publish; new brands get no model picker), #115 Playwright smoke pack (`npm run test:e2e`, 4 journeys, wired into CI after build; **local runs touch generated files — DATA_DIR isolates only the DB; seeding is DIRECT-to-DB because the colour-create API fires publishCatalog(), which would rebuild dist from the scratch DB mid-run**). #116 staging remains gated on the owner's infra/cost decision. Lesson re-learned twice this phase: scripted str.replace over a file replaces ALL matches — a global replace corrupted an unrelated test's assertion and a test selector; prefer anchored Edit calls |
| Phase-5 quote cluster (2026-08-31) | Deployed + live-verified — #81 guided quote form (service-type choice, structured fields, review-before-submit; absorbs merged #80/#83/#92), #82 multi-file (`design_request_files`, 5+5 cap, retention prune covers it), #86 tokenized status page (`design-request-status.html`, in vite htmlEntries) + account "Your Design Requests" (client session attached at submission), #87 quote→accept→deposit on the Payfast rail (`PUT /api/design-requests/:id/quote` emails via `designRequestQuoted`; accept creates a real order for `settings.quoteDepositPct` — default 50%, 100 = full — and hands to Payfast; only-from-quoted, only-once). New templates: `designRequestReceived` (ack + status link, sent on every submission), `designRequestQuoted`. A LABELED TEST REQUEST exists on prod (Johan, "TEST — quote flow") for the owner to click through admin-quote→accept→pay, then delete. Admin-side quote panel not yet click-tested with a real admin session |
| Phase-4 guidance layer (2026-08-31) | Deployed — #73 materials-guide.html (family prose + spec table generated from the filament specs themselves; sidebar/search/sitemap), #76 purchase FAQ accordions + FAQPage JSON-LD on catalogue pages, #89 DFM checklist on design-request, #90 (+merged #91) design-file retention: `designFileRetentionMonths` setting (default 12), daily audit-logged prune of finalized requests' uploads via `pruneExpiredDesignFiles` (rows/text kept; active requests never touched), confidentiality + retention wording added to privacy/terms **in the src/data/legal SOURCE** (queued for #28 Enrico review), #110 three local-SEO landing pages (generated, live catalogue counts), #108 a11y baseline in docs/ACCESSIBILITY.md (findings: muted `/45`–`/55` tints and terracotta small text fall below AA — palette calls left to owner). **The vite htmlEntries trap struck again**: the four new root pages 404'd live until added to vite.config.js (`3c826ff`) — any new root-level page MUST be hand-listed there. #71 (install guidance) deliberately not built: its mechanics wait until the owner is ready to author actual guides. Guide + SEO-page prose needs an owner read-through |
| Phase-3 discovery + commerce (2026-08-31) | Deployed — #39 store-wide search (`/search-index.json` per publish, sidebar box on every page + homepage header on the shared engine, `src/js/search.js`), #40 filament colour filters (URL-persisted), #43 back-in-stock email alerts (`restock_subscriptions` table, `server/restock.js`, subscribe control on out-of-stock swatches, notify-once + failed-send retry, triggers: publish/cancel-restore/daily sweep; **WhatsApp dormant until #7**), #50 value-prop strip, #60 volume price breaks (**SHIPPED INERT — `settings.volumeDiscounts` empty; owner must configure tiers in Settings → Storefront to activate**; server-applied in `createOrder` to the filament portion, client displays mirror via `src/js/volume-discount.js`), #66 fulfilment labels on category cards, #96 Buy Again, #97 account invoices/tracking + `orderShipped` email on the empty→set tracking transition. New email templates: `orderShipped`, `restockAlert`. **Incident during deploy: `git add -A -- public/` swept 662 untracked upload files into commit `29cb7ee`, blocking the VPS pull — fixed by `git rm --cached` in `294005c`; never `git add -A` a path that contains `public/uploads`.** Admin-side pieces (#96/#97 buttons, volume-tier editor, Communications rows) not yet click-tested with a real admin session |
| Analytics owner-exclusion + reset, cart-clear fix (2026-08-31) | Deployed — beacons carrying a valid ADMIN session are dropped server-side; `?analytics=off` on any page sets a persistent per-browser opt-out (`?analytics=on` clears). **Standing rule for AI assistants: before browsing production for verification, load any page with `?analytics=off` once in your browser session** — keeps assistant traffic out of the owner's funnel numbers. All analytics data was reset to zero on 2026-08-31 at owner request (496 views/60 visitors removed, backup `data/backups/pre-analytics-reset-2026-08-31.db`) so numbers start clean post-exclusion. Homepage cart clearing is now age-gated (24h untouched = stale; every cart write stamps `lapanza-cart-touched`) — the old unconditional clear wiped active carts on any home round-trip |
| Phase-2 performance + SEO (2026-08-31) | Deployed — #104(+105) hero-scene chunk split (homepage JS 518KB→2KB; Three.js idle-loaded, never for reduced-motion), nginx gzip + immutable /assets/ caching (production served EVERYTHING uncompressed before — conf.d/gzip.conf, live conf edited with backup, template updated), #106 sharp WebP variant pipeline (480/960 beside every upload, `<picture>`/srcset in generator, featured cards prefer 480 — worst homepage image 3,018KB→9KB; backfill script scripts/generate-image-variants.mjs, re-runnable), #109 SEO pack (JSON-LD LocalBusiness/BreadcrumbList/ItemList+Offers, canonical, og:url, robots.txt, sitemap.xml — all generator-emitted), #113 first-party funnel events (analytics_events table, fixed vocabulary, payment_complete server-side in ITN dedupe, admin funnel table). Before/after evidence: docs/PERFORMANCE.md. **Deploy-order gotcha learned: syncPublicJson must run AFTER generate-image-variants on a fresh deploy, or featured-product image paths resolve to originals** (bit once, fixed by re-running sync+build). Legal page text lives in src/data/legal/ — editing root privacy.html etc. gets clobbered by the next generate |
| Phase-1 quick wins (2026-08-31) | Deployed — #62 cart delivery estimate, #36/#136 hero CTA hierarchy (Shop Filament primary / quote secondary / WhatsApp text link), #72 vehicle part-request prefill (`?context=`), #24 client PUDO locker fields (+ New Order panel display), #134 print-job batch quantity (per-copy inputs scale; design/setup one-off), #135 inline new-roll add, #126 scroll+focus new catalog rows, #65 guest-checkout copy. Public items verified live; **admin items (#24 #126 #134 #135) still need a real admin click-through**. #137 awaits owner's parking/collection wording. Deploy note: VPS generated-file drift blocked the pull — resolved per the standing runbook (reset generated files → pull → `syncPublicJson()` from live DB → `npm run generate` → build), never `git stash -u` |
| Print Job Costing: Final Selling Price + "List for sale" | Live — status renamed `Printed`/`Estimate`; new admin-editable Final Selling Price (defaults to the computed Minimum Selling Price); a job can be explicitly published as a real category product and stays linked so re-listing updates the same item's stock/price instead of duplicating it. See `listPrintJobForSale`/`updatePrintJobListing` in `server/print-jobs.js` |
| Historical print-job import | `scripts/import-historical-print-jobs.mjs` -- one-time backfill of 124 pre-existing print jobs (from a spreadsheet, before this feature existed) into `print_jobs`/`print_job_filaments`. Run once against local dev already; **not yet run against production** -- see the script's header comment for usage and why it deliberately bypasses `createPrintJob()` (doesn't decrement current in-house-filament stock, allows >4 filament slots for jobs that historically used more). Not idempotent -- don't re-run against a DB that already has its output. |
| Stock Management "Listed on products page" | Live — radio per row (filament colours + category items) in the admin Stock Management grid, alongside the existing stock/price editors. `0`/unchecked pulls just that item off its public grid (filament colour grid or category page) at the next `generate-pages.mjs` run, without touching stock/price, the category-item `available` (add-to-cart) flag, or a filament type's own draft/published `status`. See `server/inventory.js`'s `listed` field, `filament_colours.listed` column |
| Audit Logs page | Live — new `audit_log` table + `server/audit-log.js`, `GET /api/audit-log` (filterable by `eventType`/`q`), admin "Audit Logs" page (Settings group). Records every admin login (success/failure), logout, session expiry, and admin-account change (create/delete/password reset) -- append-only, newest first. Scoped to the admin portal's own session/account security, not a general action-audit of every CRUD write in the app |
| Print Job Costing: attachments column + Hours/Minutes print-time entry | Live — a job's model file and reference photo can now be attached/replaced from the table at any time (previously only possible at creation), with the original filename shown and a real Download link/button; new `reference_file_original_name`/`reference_image_original_name` columns store that filename separately from the randomized on-disk path. Print time is now captured as separate Hours + Minutes fields, combined into the existing `printTimeMinutes` client-side before it reaches the API/DB (no schema change to that field). No working "open directly in Bambu Studio/OrcaSlicer" link exists for a self-hosted file -- Bambu Studio's own `bambustudio://open` scheme is hard-locked to makerworld.com URLs and `.3mf` only; Download + the OS's own file association is the realistic equivalent |
| Public-site "Back to Home" buttons | Live — filament and category (toys/homeware/phones/car-parts) detail pages now show a button-styled "← Back to Home" link both directly under the breadcrumb and again at the bottom of the page (`backToHomeButton()` in `scripts/generate-pages.mjs`), addressing reported dead-ends on long pages / mobile (sidebar catalogue nav is hidden behind the hamburger menu there). Also fixed every filament page's breadcrumb "Filament" segment, which previously linked to `../filament/pla.html` regardless of which filament page you were on -- now links to `index.html#range` |
| Backlog sweep + "Claude Fix" status | New Todo/Backlog status distinct from `Done`, marking items this assistant resolved directly. Closed: legal pages (backdated), cookie disclosure (via the Privacy Policy), print-job stock-sufficiency warning (`stockWarnings()` in `server/print-jobs.js` -- non-blocking by design, since the historical import intentionally logs jobs exceeding recorded stock), unused `uuid` dependency (confirmed zero import sites, removed). Two new backlog items logged: design-requests/3D-resources uploads need the same lost-original-filename fix print-jobs just got, and `audit_log`/`page_views` have no retention/pruning job |
| Legal pages: sole prop → partnership (closes #29) | Live -- confirmed with the owner that Linandi Barkhuizen is a real co-owner (a sole proprietorship can't legally have two owners). terms.html/privacy.html now describe Lapanza as a partnership owned by Johan Barkhuizen and Linandi Barkhuizen; Johan stays the sole POPIA Information Officer |
| Widened audit log + security signals | Live -- backlog #27. `AUDIT_EVENTS` extended with `order_updated`/`stock_updated`/`catalog_updated`/`settings_updated` (one broad type per area, specifics in `detail`) instrumented across ~24 admin routes, plus three security signals: `client_login_failure`, `rate_limit_exceeded` (authLimiter/publicFormLimiter/checkoutLimiter, not analyticsLimiter), and `unauthorized_access` (only when there's no session cookie at all -- excludes the routine post-restart "cookie present but unknown" case, Won't Fix #4). Passive logging only, no email alerts. `audit_log` now prunes entries older than 12 months daily (`startAuditLogPruneJob`), closing tech-debt #32 for that table |
| page_views retention (closes #32) | Live -- `pruneOldPageViews()`/`startPageViewsPruneJob()`, same shape as audit_log's. Two new permanent tally tables (`analytics_page_totals`, `analytics_seen_visitors`) keep the Analytics dashboard's "all-time" totals/top-pages/unique-visitors accurate after old `page_views` rows are pruned -- without them, "all-time" would have quietly become "since the last prune". A one-time backfill (`backfillAnalyticsTotals()`) populates both from existing `page_views` history on first boot after this shipped, so the dashboard didn't show a false reset to the VPS's real historical traffic |
| Original-filename tracking for resources & design requests (closes #31) | Live — `image_original_name`/`file_original_name` on `resources`, `reference_image_original_name`/`reference_file_original_name` on `design_requests`, same pattern as `print_jobs`. Admin UI shows/downloads-as the real filename instead of the randomized storage name |
| Category-item photo upload + "Visible on site" toggle (closes #33) | Live — Toys/Homeware/Phones/Car-Parts items get real upload/replace/remove photo (was a plain Image URL text field) and a `listed` checkbox (was already read by `generate-pages.mjs`/`export.js`, just never exposed in the admin UI). `POST`/`PUT`/`DELETE /api/products` now call `syncPublicJson()` — they never did before, so edits silently never reached `src/data/categories.json` |
| Consistent "R 350.00" currency formatting | Live — see the `money.js` non-obvious-thing above. Fixed the whole site: static pages, cart/checkout, admin (~20 views), order emails, invoices |
| Checkout page cleanup (closes #127) | Live — consent checkboxes moved to the bottom; "Update Details" is a real button, hidden until a logged-in customer actually changes a detail field, saves via new `PATCH /api/client/me` (self-service, explicit field allow-list — admin-only fields like `discountPct` never reachable); added a Cancel Order button; shipping/payment/PUDO-option selection now persists to `localStorage` across page reloads (was silently resetting every time) |
| Order reference shortened, banking details shown on Manual EFT (closes #128) | Live — customer-facing order reference is the first 8 chars everywhere (was the full 36-char UUID, unusable as an EFT reference); Manual EFT confirmation panel now shows the actual banking details, not just "check your email" |
| Checkout: PUDO default, unreadable dropdown, consent re-placed, Payfast confirmation timing (closes #129) | Live — PUDO locker/local delivery is now first in Shipping Options and the default (was courier); its fixed-option `<select>` was unreadable (`bg-transparent`, no explicit text colour) and now uses explicit `bg-cream`/`text-charcoal`; consent checkboxes moved again, now directly below Your Details (was the page bottom per #127); and the important one — see the "Order confirmation emails must never send before payment is actually confirmed" non-obvious-thing above |
| Configurable lists | Live — in-house filament brands, Todo/Backlog Category + Priority are admin-editable in Settings with an active/inactive toggle (`{id,name,active}[]`, `configurableListPanel()` in `admin.js`). Deliberately NOT extended to order status/payment method/shipping type/print-job status/purchase status/design-request status/audit event types/Todo Status — code branches on those exact strings, an admin-typed new value wouldn't have any logic behind it. Car-part brands (GWM/Landrover) considered and deferred — bigger scope, needs dynamic page generation |
| Delivery expectations (closes #44) | Live — filament pages, category pages, and the cart drawer show real dispatch/production windows (`filamentDispatchDays` "1-2", `printLeadTimeDays` "3-5" business days, both admin-editable, both real figures from the owner). Requires a re-publish to apply to already-generated filament/category pages, same as low-stock messaging |
| Low-stock messaging (closes #61) | Live — filament colours show "Only N left" at/below `lowStockThreshold` (Settings, default 3), plain "In stock" above it, "Out of stock" at 0. Requires a re-publish (`npm run generate` or admin Publish to site) to apply to already-generated pages — it's a generation-time template change, not a runtime one |
| Checkout trust badges (closes #54) | Live — secure Payfast checkout / POPIA-compliant / faulty-or-damaged-items-covered badges above the Place Order button, wording checked against the real Returns Policy (not a blanket "money-back guarantee" — most items are custom, non-returnable for change of mind) |
| Backup restore procedure (closes #119) | Documented and validated (`deploy/DEPLOY.md` §10) — a real backup file passed `PRAGMA integrity_check` plus row-count checks. The full live stop/swap/restart cutover is still unrehearsed |
| Order data | 18 cancelled test orders (`INV-0009`–`INV-0026`) hard-deleted at owner request 2026-08-26 — pre-delete snapshot kept, audit-logged. Next real order will be `INV-0009`, not a continuation of the deleted test numbers |
| Test suite | 344/344 passing (`node --test`, 35 test files), run before every commit |
| CI/CD | Test-only: `.github/workflows/test.yml` runs `npm ci && npm test && npm run build` on every push/PR to main. Deploy remains manual test-then-push-then-SSH (`bash deploy/deploy-app.sh`) -- automating that needs the VPS SSH key as a GitHub secret, an infra/security decision left to the business owner |
| Staging environment | None — the VPS is production from first deploy |
| Branded HTML emails + Settings → Communications (V0.69–V0.70) | Live — every transactional email except the invoice was plain text; all 9 now render through a shared branded HTML shell (`server/email-template.js`). Subject/message wording per email is admin-editable (`settings.emailTemplates`, `{{token}}` placeholders); structural HTML (links, tables, the reset-link expiry disclaimer) stays code-controlled. Click-tested live with real admin access — a genuine password-reset send confirmed working |
| Operational alerts (V0.72–V0.73, closes backlog #120) | Live — `server/alerts.js` escalates backup failures, Payfast ITN failures, unexpected checkout errors, an email-delivery-down WhatsApp fallback, and security-signal bursts (including admin login failures, added after an initial gap was caught reviewing the item against its own spec). New Settings → Operational Alerts panel (toggles/thresholds) and an unauthenticated `GET /api/health/backups` heartbeat for a second uptime monitor. WhatsApp fallback needs a Meta-approved template the owner must still set up — degrades to console-only until then. Response runbook: `deploy/DEPLOY.md` §11 |
| Admin-managed testimonials (V0.74, closes backlog #51) | Live — new `testimonials` table + admin CRUD (Client Side nav). A testimonial can't be set to `published` without `consent_given=true`, enforced in `server/testimonials.js` itself (not just the admin UI). `customer_name` (real name, internal) is kept separate from `display_name` (what's shown). Published testimonials ride along in `site-settings.json` (same mechanism as `featuredProducts`), explicitly excluding the real name/consent note. New homepage section, hidden until something's published. Verified live with a real create/publish/verify/delete cycle, no leftover data |
| Homepage fixes (V0.68, V0.71) | Live — fixed a real CSS bug squashing featured-product images to ~232×56px strips (a fixed height utility was fighting `md:aspect-square`), enlarged the hero flanking columns, removed the dead "Shop the range" tile section, removed "Centurion" from the hero eyebrow line |
| Sidebar tagline removed (V0.79) | Live — the "Creative Lab · Centurion" line under the Lapanza 3D brand mark in the sitewide sidebar (`src/js/nav.js`) removed at owner request; the V0.71 change had only covered the homepage hero copy |
| Launch-audit High batch (V0.91) | Live — Site Overview directory browsing clamped to the app root (was the whole drive); checkout hides Cash on Collection unless collecting + Place Order shows an error instead of a silent no-op when shipping isn't ready; design-request uploads validate type/size at pick time (server's multer fileFilter silently drops bad files) and nginx `client_max_body_size` raised to 50m (template AND live conf, backup `lapanza.conf.bak-20260901`); Facebook aligned everywhere to the real page + old URL fixed in the prod DB settings row (audit-logged); wrong TikTok link removed pending the real profile URL from the owner; `noindex` on admin + account login pages. Companion server-side batch (Secure cookies, security headers, auth/me TTL, campaign send-queue catch) shipped separately as `6d7db7b`. |
| Launch-audit final batch (V0.94) | Live — B5 (no cart drawer on checkout — it desynced the submitted items), B4 code-side (Add to Cart gated on stock, zero-stock reads "Out of Stock"), H2 (CORS origin allowlist), plus the Medium list: un-cancel re-reserves stock, Stock-grid optimistic concurrency (`expectedStockQty`), admin 8+ password everywhere, registration anti-enumeration (generic 201 + email to the real owner), 404 page + nginx error_page (live conf edited, backup kept), JSON error middleware for multer, resource-download active check, clear-cart confirm, breadcrumb/meta/font polish. Audit items remaining: B2 Test Product (owner, at go-live), H10 story hero image (needs owner's photo), og:image share image (needs owner's brand image) |
| Public settings allowlist (V0.92, closes launch-audit blocker #3) | Live — `publicSettings()` was a name-only pass-through, so the public `/site-settings.json` carried the bank account number, the whole print-cost model, alert thresholds, and email templates. Now an explicit allowlist (`PUBLIC_SETTINGS_KEYS` in `server/settings.js`) of the ~25 keys the storefront and `generate-pages` actually read; a NEW public-facing settings key must be added there or it silently won't reach the site. The authenticated admin `GET/PUT /api/settings` routes deliberately return the FULL object. Manual-EFT checkout success panel gets banking details from the checkout response (`bankingDetails`, manual_eft only) instead of the public file. |
| Get in Touch page (V0.67) | Live — corrected Facebook link (was pointing at the wrong page), added TikTok, added social icons, added a Google Maps embed under the workshop address |
| Contact hours / WhatsApp response / escalation guidance (closes backlog #78) | Live — `settings.hours` existed since early in the project but was never actually displayed anywhere (the contact section had "By appointment" hardcoded, disconnected from the setting) — now genuinely live via `hydrateContactInfo()` (`src/js/site.js`) on both `index.html` and `get-in-touch.html`. Two new settings, both real figures from the owner: `whatsappResponseNote`, `escalationContactsNote` — admin-editable from Settings → Public Site Contact, right next to the (also now-corrected) Hours field |

## Who to ask

The business owner (repo owner, `jbarkhuizen@gmail.com`) is the only stakeholder and the only person with server/DNS/payment-provider credentials. Any decision requiring real secrets, financial config, or DNS changes needs them directly — an AI assistant should never fabricate or guess these.

---

*Generated as a portable context primer for handing this project to a different AI system/tool. Keep it in sync with `SYSTEM_DOCUMENTATION.md` §12.5 and §15 when either changes.*
